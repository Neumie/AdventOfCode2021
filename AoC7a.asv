clear; clc; close all;

input = fileread('inputAoC7test.txt'); %input který nám dal AoC skopírovaný do .txt souboru
data = regexp(input, '[,]', 'split');
for i = 1:length(data)
    data{i} = str2double(data{i});
end
data = cell2mat(data);

min = min(data)
max = max

for i = 1:length(data)

end